# Copy outside version control as .beta-runtime.env and populate through the
# approved secret-management path. Never commit the populated file.
BETA_ACCESS_ENABLED=true
VITE_BETA_ACCESS_ENABLED=true
PLAYER_HEADSHOTS_ENABLED=false
BETA_ACCESS_CODE_HMAC_SECRET=replace-with-secret
BETA_ACCESS_RESERVATION_SECRET=replace-with-secret
VITE_API_BASE_URL=/api
